name: workflow_events

on:
    push:
        branches:
        - master
    pull_request:
        branches:
        - master

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Echo
              run: echo "I 've traggard by a ${{github.event_name}}!"

   
